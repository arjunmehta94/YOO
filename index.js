/*** @jsx React.DOM */
var Alert = ReactBootstrap.Alert;
var NavBar = React.createClass({
	render : function() {
		return (
			<h1>adsf</h1>
			//console.log(Alert)
		);
	}
});

React.render(<NavBar/>, document.getElementById('mount-point'));